const cursor = document.querySelector('cursor');

window.addEventListener("mousemove", e => {
  const cursor.style.left = e.pageX + "px";
  const cursor.style.top = e.pageX + "px";
})

window.addEventListener("mousemove", e => {
  const cursor.style.riight = e.pageX + "px";
  const cursor.style.top = e.pageX + "px";
})